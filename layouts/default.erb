<!DOCTYPE HTML>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Blag: <%= @item[:title] %></title>
    <% for item in @items.select{|i| i.identifier.components.first == 'stylesheets' } %>
      <link rel="stylesheet" href="<%= item.identifier.without_ext %>.<%= Digest::SHA1.hexdigest(item.raw_content) %>.css">
    <% end %>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
  </head>
  <body class="regular">
    <div id="main">
      <div id="content">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
